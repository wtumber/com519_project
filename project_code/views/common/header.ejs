<div class="container">
    <header class="blog-header py-3">
      <div class="row flex-nowrap justify-content-between align-items-center">
        <div class="col-4 pt-1">
          <% if(user) { %>
          <a class="btn btn-sm btn-outline-secondary" href="logout">Log out</a>
          <% } %> <% if(!user) { %>
          <a class="btn btn-sm btn-outline-secondary" href="login">Log in</a>
          <a class="btn btn-sm btn-outline-secondary" href="signup">Sign up</a>
          <% } %>
        </div>
        <div class="col-4 text-center">
          <a class="blog-header-logo text-dark" href="/">Dambry</a>
        </div>
        <div class="col-4 d-flex justify-content-end align-items-center">
          <div class="input-group">
            <input type="text" class="form-control" placeholder="Search database for...">
            <div class="input-group-btn">
              <button class="btn btn-outline-secondary" type="button">Go!</button>
            </div>
          </div>
        </div>
      </div>
    </header>
  
    <div class="nav-scroller py-1 mb-2">
      <nav class="nav d-flex justify-content-between">
        <a class="p-2 link-secondary" href="/">Home</a>
        <a class="p-2 link-secondary" href="guides">Resources</a>
        <a class="p-2 link-secondary" href="recommenders">Users</a>
        <% if(user) { %>
        <a class="p-2 link-secondary" href="add_resource">Add new resource</a>
        <%}%>
      </nav>
    </div>
  </div>